@model IEnumerable<RedSocial.Models.Media>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        
        <th>
            @Html.DisplayNameFor(model => model.NombreFoto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ArchivoURL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NomArchivoFoto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DetalleFoto)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdMedia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombreFoto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ArchivoURL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NomArchivoFoto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DetalleFoto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdUsuario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.IdMedia }) |
            @Html.ActionLink("Details", "Details", new { id = item.IdMedia }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.IdMedia })
        </td>
    </tr>
}

</table>
